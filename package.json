{
  "name": "spine-viewer",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:v8": "tsc && vite build",
    "build:v7": "cd v7 && npm install && npx tsc --noEmit && vite build",
    "build:all": "npm run build:v8 && npm run build:v7",
    "dev:v7": "vite serve v7 --config v7/vite.config.ts",
    "preview": "vite preview",
    "deploy": "npm version minor && npm run build:all && gh-pages -d dist && git push --follow-tags"
  },
  "dependencies": {
    "@esotericsoftware/spine-pixi-v8": "~4.2.0",
    "jszip": "^3.10.1",
    "pixi.js": "^8.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
